<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Trip Report - <%= @trip.name %></title>
  <%= wicked_pdf_stylesheet_link_tag 'pdf' %>
  <style type="text/css">
    body {
      font-family: sans-serif;
      font-size: 12px;
      line-height: 1.6;
      color: #333;
    }
    h1, h2, h3, h4 {
      color: #1a202c;
      margin-top: 1em;
      margin-bottom: 0.5em;
    }
    h1 { font-size: 24px; }
    h2 { font-size: 20px; }
    h3 { font-size: 16px; }
    .header {
      text-align: center;
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 1px solid #eee;
    }
    .section {
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 1px dashed #eee;
    }
    .section:last-child {
      border-bottom: none;
    }
    .journal-entry {
      border: 1px solid #eee;
      padding: 10px;
      margin-bottom: 10px;
      border-radius: 5px;
    }
    .expense-item {
      display: flex;
      justify-content: space-between;
      padding: 5px 0;
      border-bottom: 1px dotted #eee;
    }
    .expense-item:last-child {
      border-bottom: none;
    }
    .total {
      font-weight: bold;
      font-size: 14px;
      text-align: right;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>Trip Report: <%= @trip.name %></h1>
    <p><%= @trip.description %></p>
    <p>Dates: <%= @trip.start_date.strftime("%b %d, %Y") %> - <%= @trip.end_date.strftime("%b %d, %Y") %></p>
  </div>

  <% if @journal_entries.any? %>
    <div class="section">
      <h2>Journal Entries</h2>
      <% @journal_entries.each do |entry| %>
        <div class="journal-entry">
          <h3><%= entry.entry_date.strftime("%b %d, %Y") %> - <%= entry.location_name %></h3>
          <p><%= entry.content %></p>
          <p><em>by <%= entry.user.email_address %></em></p>
        </div>
      <% end %>
    </div>
  <% end %>

  <% if @expenses.any? %>
    <div class="section">
      <h2>Expenses</h2>
      <% @expenses.each do |expense| %>
        <div class="expense-item">
          <span><%= expense.description %> (by <%= expense.payer.email_address %>)</span>
          <span><%= "%.2f" % expense.amount %> EUR</span>
        </div>
      <% end %>
      <div class="total">
        Total Expenses: <%= "%.2f" % @trip.total_expenses %> EUR
      </div>
    </div>
  <% end %>

  <div class="section">
    <h2>Summary</h2>
    <p>Total Journal Entries: <%= @journal_entries.count %></p>
    <p>Total Expenses: <%= "%.2f" % @trip.total_expenses %> EUR</p>
  </div>
</body>
</html>