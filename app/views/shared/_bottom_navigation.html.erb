<nav class="fixed bottom-0 left-0 right-0 bg-gradient-to-r from-amber-50 to-orange-50 border-t border-amber-200 md:hidden z-50 backdrop-blur-sm">
  <div class="flex justify-around py-3">
    <% nav_items = [
      { path: root_path, icon: "🏠", label: "Home", active: current_page?(root_path) },
      { path: recipe_library_path, icon: "📚", label: "Recipes", active: current_page?(recipe_library_path) },
      { path: "/plan", icon: "📋", label: "Plan", active: params[:phase] == "plan" || current_page?("/plan") },
      { path: "/go", icon: "🏃", label: "Go", active: params[:phase] == "go" || current_page?("/go") },
      { path: "/reminisce", icon: "💭", label: "Reminisce", active: params[:phase] == "reminisce" || current_page?("/reminisce") }
    ] %>
    
    <% nav_items.each do |item| %>
      <%= link_to item[:path], class: "flex flex-col items-center py-2 px-4 rounded-xl transition-all duration-300 #{item[:active] ? 'bg-amber-600 text-white shadow-lg transform scale-105' : 'text-amber-800 hover:bg-amber-100 hover:scale-105'}" do %>
        <span class="text-2xl mb-1 filter drop-shadow-sm"><%= item[:icon] %></span>
        <span class="text-xs font-semibold tracking-wide"><%= item[:label] %></span>
      <% end %>
    <% end %>
  </div>
</nav>