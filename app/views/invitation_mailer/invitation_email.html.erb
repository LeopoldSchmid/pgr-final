<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <style>
    /* Email styling */
    body { font-family: Arial, sans-serif; background-color: #f4f4f4; margin: 0; padding: 0; }
    .container { max-width: 600px; margin: 20px auto; background-color: #ffffff; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); }
    .header { text-align: center; padding-bottom: 20px; border-bottom: 1px solid #eeeeee; }
    .header h1 { color: #333333; font-size: 24px; margin: 0; }
    .content { padding: 20px 0; line-height: 1.6; color: #555555; }
    .button { display: inline-block; padding: 10px 20px; margin: 10px 0; background-color: #6a0dad; color: #ffffff; text-decoration: none; border-radius: 5px; }
    .footer { text-align: center; padding-top: 20px; border-top: 1px solid #eeeeee; font-size: 12px; color: #aaaaaa; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>PlanGoReminisce</h1>
    </div>
    <div class="content">
      <p>Hello!</p>
      <p><%= @invited_by.email_address %> has invited you to join their trip: <strong><%= @trip.name %></strong>!</p>
      <p>
        <strong>Trip Description:</strong> <%= @trip.description %>
      </p>
      <p>
        <strong>Your Role:</strong> You've been invited as a <strong><%= @invitation.role.capitalize %></strong>.
      </p>
      <p>
        Click the button below to accept the invitation and join the trip:
      </p>
      <p style="text-align: center;">
        <a href="<%= @accept_url %>" class="button">Accept Invitation</a>
      </p>
      <p>
        If you don't want to join, you can decline here:
      </p>
      <p style="text-align: center;">
        <a href="<%= @decline_url %>" class="button" style="background-color: #dc3545;">Decline Invitation</a>
      </p>
      <p>This invitation will expire on <%= @invitation.expires_at.strftime("%B %d, %Y at %I:%M %p %Z") %>.</p>
      <p>See you there!</p>
      <p>The PlanGoReminisce Team</p>
    </div>
    <div class="footer">
      <p>&copy; <%= Time.current.year %> PlanGoReminisce. All rights reserved.</p>
    </div>
  </div>
</body>
</html>