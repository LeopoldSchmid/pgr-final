<div class="container mx-auto px-4 py-6">
  <!-- Header -->
  <div class="mb-6">
    <%= link_to @trip, class: "text-purple-700 hover:text-purple-900 font-medium flex items-center group mb-4" do %>
      <span class="transform group-hover:-translate-x-1 transition-transform mr-2">👥</span> Back to <%= @trip.name %>
    <% end %>
    <h1 class="text-3xl font-bold bg-gradient-to-r from-purple-800 to-pink-700 bg-clip-text text-transparent">
      Invite Friends
    </h1>
    <p class="text-purple-700 mt-2">Send an invitation to join <%= @trip.name %></p>
  </div>

  <!-- Invitation Form -->
  <div class="bg-gradient-to-r from-white to-purple-50 rounded-2xl shadow-lg border border-purple-200 p-8">
    <%= form_with model: [@trip, @invitation], local: true, class: "space-y-6" do |form| %>
      
      <!-- Email -->
      <div>
        <%= form.label :email, class: "block text-purple-700 font-medium mb-2" do %>
          📧 Email Address
        <% end %>
        <%= form.email_field :email, 
            placeholder: "friend@example.com",
            class: "w-full p-3 border border-purple-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent" %>
        <% if @invitation.errors[:email].any? %>
          <p class="text-red-500 text-sm mt-1"><%= @invitation.errors[:email].first %></p>
        <% end %>
      </div>

      <!-- Role -->
      <div>
        <%= form.label :role, class: "block text-purple-700 font-medium mb-2" do %>
          🔑 Role
        <% end %>
        <%= form.select :role, 
            options_for_select([
              ['👥 Guest - Can view trip details and journal entries', 'guest'],
              ['👤 Member - Can view and add expenses', 'member'],
              ['👑 Admin - Can manage expenses and invite others', 'admin']
            ], @invitation.role),
            {},
            class: "w-full p-3 border border-purple-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent" %>
        <p class="text-purple-600 text-sm mt-1">Guests can view the trip, members can participate in expenses, admins can manage everything</p>
      </div>

      <!-- Info Box -->
      <div class="bg-blue-50 border border-blue-200 rounded-xl p-4">
        <div class="flex items-start space-x-3">
          <span class="text-blue-500 text-xl">💡</span>
          <div class="text-blue-800">
            <h4 class="font-semibold mb-2">How invitations work:</h4>
            <ul class="text-sm space-y-1">
              <li>• Your friend will receive an invitation link</li>
              <li>• They can create an account or sign in with an existing account</li>
              <li>• Once accepted, they'll be able to participate in expenses</li>
              <li>• Invitations expire after 7 days</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- Submit Button -->
      <div class="text-center pt-4">
        <%= form.submit "💌 Send Invitation", 
            class: "bg-gradient-to-r from-purple-600 to-pink-600 text-white px-8 py-4 rounded-xl font-bold hover:from-purple-700 hover:to-pink-700 transition-all duration-200 shadow-lg transform hover:scale-105 text-lg" %>
      </div>
    <% end %>
  </div>
</div>
